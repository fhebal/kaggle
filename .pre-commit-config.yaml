repos:
  # - repo: https://github.com/psf/black
  #   rev: 24.4.0  # Use a specific version or 'stable' for the latest stable release
  #   hooks:
  #     - id: black
  #       args: ['--line-length=120']
  #       language_version: python3
  - repo: https://github.com/pycqa/isort
    rev: '5.13.2'  # Specify the version you want to use
    hooks:
      - id: isort
        args: ['--profile=black']
        name: isort (Python imports)
        types: [python]
  - repo: https://github.com/pycqa/flake8
    rev: '7.0.0'  # Specify a version
    hooks:
      - id: flake8

